# abort script on any command that exits with a non zero value
set -e

# untar pcre source files
tar xzf pcre/pcre-8.40.tar.gz

# push folder in temp
pushd pcre-8.40
  ./configure
  make
  make install
popd
